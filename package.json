{
  "name": "axinstagram",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "cf-typegen": "wrangler types",
    "test": "bun test",
    "format": "prettier --write \"src/**/*.ts\" \"functions/**/*.ts\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "prettier": "^3.8.3",
    "typescript": "^5.7.2",
    "wrangler": "^3.101.0"
  }
}
